{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Debug Flask Path",
			"type": "shell",
			"command": "python",
			"args": [
				"-c",
				"import os; backend_dir = r'c:\\Users\\chama\\Desktop\\LLM_Chatbot_With_Agent\\Detect_Na\\dashboard\\backend\\app.py'; frontend_dir = os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(backend_dir))), 'frontend'); index_file = os.path.join(frontend_dir, 'index.html'); print(f'Frontend dir: {frontend_dir}'); print(f'Index exists: {os.path.isfile(index_file)}'); print(f'Size: {os.path.getsize(index_file) if os.path.isfile(index_file) else 0}')"
			]
		}
	]
}